/**
 * Well-known tutorial ids, shared between the trigger points (setup flow,
 * Settings "replay" action) and the future step definitions registered via
 * registerTutorial(). Steps for this tutorial are not defined yet.
 */
export const ONBOARDING_TUTORIAL_ID = "onboarding";
